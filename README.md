# Album API

Album API written in go.

## Instalation:
Clone the repository then run with docker-compose
> docker compose up

## Endpoints
> GET at /albums<br>
> GET by ID at /albums/id<br>
> POST at /albums